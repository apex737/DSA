#include <bits/stdc++.h>
using namespace std;

struct Pos
{
    int r, c;
};

int N, M;

Pos monster[4];
Pos person[4];

// [0 ~ M-1]     : 몬스터
// [M ~ 2*M-1]   : 의뢰인
Pos eventPos[8];

// memo[처리한 이벤트 집합][현재 위치]
int memo[1 << 8][9];

int fullMask;


// ========================================================
// TODO 1.
// 두 좌표 사이의 맨해튼 거리
// ========================================================
int dist(Pos a, Pos b)
{
    // TODO 1
    return 0;   // dummy
}


// ========================================================
// TODO 2.
// cur의 실제 좌표 반환
//
// cur == 2*M 이면 시작점 (0,0)
// 아니면 eventPos[cur]
// ========================================================
Pos getPos(int cur)
{
    // TODO 2
    return {0, 0};   // dummy
}


// ========================================================
// go(mask, cur)
//
// mask만큼 이벤트를 처리했고,
// 현재 cur 위치일 때
// 앞으로 모든 일을 끝내는 최소 추가 거리
// ========================================================
int go(int mask, int cur)
{
    // ====================================================
    // TODO 3. 종료조건
    // 모든 이벤트 완료라면?
    // ====================================================
    if (false)   // TODO 3
    {
        return 0;   // dummy
    }


    // ====================================================
    // TODO 4. 메모이제이션
    // ====================================================
    int& ret = memo[0][0];   // TODO 4 - dummy

    if (false)   // TODO 4
    {
        return ret;
    }


    // ====================================================
    // TODO 5.
    // ret을 충분히 큰 값으로 초기화
    // ====================================================
    ret = 0;   // TODO 5 - dummy


    // ====================================================
    // TODO 6.
    // 현재 위치 좌표 얻기
    // ====================================================
    Pos now = {0, 0};   // TODO 6 - dummy


    // ====================================================
    // 1. 아직 안 잡은 몬스터 선택
    // ====================================================
    for (int i = 0; i < M; i++)
    {
        // ================================================
        // TODO 7.
        // 이미 잡은 몬스터면 continue
        // ================================================
        if (false)   // TODO 7
            continue;


        // ================================================
        // TODO 8.
        // i번 몬스터를 잡은 새로운 mask
        // ================================================
        int nxtMask = mask;   // TODO 8 - dummy


        // ================================================
        // TODO 9.
        //
        // 현재 → i번 몬스터 거리
        // +
        // 다음 상태의 최소거리
        // ================================================
        ret = min(
            ret,
            0   // TODO 9 - dummy
        );
    }


    // ====================================================
    // 2. 잡은 몬스터에 대응하는 의뢰인 방문
    // ====================================================
    for (int i = 0; i < M; i++)
    {
        int monsterBit = i;
        int personBit = M + i;


        // ================================================
        // TODO 10.
        // 몬스터를 아직 안 잡았다면 갈 수 없음
        // ================================================
        if (false)   // TODO 10
            continue;


        // ================================================
        // TODO 11.
        // 이미 의뢰 완료했다면 continue
        // ================================================
        if (false)   // TODO 11
            continue;


        // ================================================
        // TODO 12.
        // 의뢰 완료 비트까지 켠 새로운 mask
        // ================================================
        int nxtMask = mask;   // TODO 12 - dummy


        // ================================================
        // TODO 13.
        //
        // 현재 → 의뢰인 거리
        // +
        // 다음 상태의 최소거리
        // ================================================
        ret = min(
            ret,
            0   // TODO 13 - dummy
        );
    }


    // ====================================================
    // TODO 14.
    // 현재 상태의 최소거리 반환
    // ====================================================
    return 0;   // TODO 14 - dummy
}


int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // ====================================================
    // TODO 15.
    // 테스트케이스 개수 입력
    // ====================================================
    int T = 0;   // TODO 15 - dummy


    for (int tc = 1; tc <= T; tc++)
    {
        // =================================================
        // TODO 16.
        // N 입력
        // M 초기화
        // =================================================
        N = 0;   // TODO 16 - dummy
        M = 0;


        // =================================================
        // STEP 1.
        // 맵 입력
        // =================================================
        for (int r = 0; r < N; r++)
        {
            for (int c = 0; c < N; c++)
            {
                int x = 0;
                cin >> x;

                if (x > 0)
                {
                    // =====================================
                    // TODO 17.
                    // 몬스터 좌표 저장
                    // M 갱신
                    // =====================================
                    // dummy: 아무것도 안 함
                }
                else if (x < 0)
                {
                    // =====================================
                    // TODO 18.
                    // 의뢰인 좌표 저장
                    // M 갱신
                    // =====================================
                    // dummy: 아무것도 안 함
                }
            }
        }


        // =================================================
        // STEP 2.
        // eventPos 구성
        // =================================================
        for (int i = 0; i < M; i++)
        {
            // TODO 19
            eventPos[i] = {0, 0};       // dummy
            eventPos[M + i] = {0, 0};   // dummy
        }


        // =================================================
        // STEP 3.
        // 모든 이벤트가 완료된 fullMask
        // =================================================
        fullMask = 0;   // TODO 20 - dummy


        // =================================================
        // STEP 4.
        // memo 초기화
        // =================================================
        memset(memo, 0, sizeof(memo));   // TODO 21 - dummy


        // =================================================
        // STEP 5.
        // 시작점 index
        // =================================================
        int start = 0;   // TODO 22 - dummy


        // =================================================
        // STEP 6.
        // 아무것도 처리하지 않은 상태에서 DP 시작
        // =================================================
        int answer = 0;   // TODO 23 - dummy


        cout << "#" << tc << " " << answer << "\n";
    }

    return 0;
}